part of 'constants.dart';

enum AppTheme { light, dark }

enum HorizontalAlignment { left, right }

enum ButtonType {
  primary,
  primaryLight,
  secondary,
  tertiary,
  tertiaryLight,
  danger,
  textOnly,
  textOnlySecondary,
  textOnlyTertiary,
}

enum OverlayLoaderType { circular, line }

enum SpacingSize {
  form,
  pageTopBottom,
  pageBottomBarBottom,
  xSmall,
  small,
  medium,
  large,
  xLarge,
  xxLarge,
}

enum FontSize {
  title,
  titleBold,
  titleBlack,
  heading,
  headingBold,
  headingBlack,
  bodyLarge,
  bodyLargeBold,
  bodyLargeBlack,
  body,
  bodyBold,
  bodyBlack,
  label,
  labelBold,
}

enum InputType { text, email, phone, integer, float, date }

enum ToastOverflow { truncated, expanded }

enum ToastType { success, info, danger }

enum InformationType { warning, info }

enum Haptics { selection, vibrate, nudge, success, error }

enum TagType { transparent, none, info, infoLight, danger }

enum ChipsArrangement { wrap, list }

enum SliderType { single, range }

enum Shadow { xSmall, small, medium, large, xLarge, xxLarge }
